{
  "title": "Reimbursement",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "user_id": {
      "type": "string",
      "format": "uuid"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "amount": {
      "type": "number"
    },
    "currency": {
      "type": "string"
    },
    "transaction_date": {
      "type": "string",
      "format": "date"
    },
    "merchant": {
      "type": "string"
    },
    "receipts": {
      "type": "array",
      "description": "Receipts listed in ascending order by their creation time, related to the reimbursement.",
      "items": {
        "type": "string",
        "format": "uuid",
        "minLength": 1
      }
    }
  }
}